{
  "name": "dictionary-app-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix client install && npm --prefix client run build"
  },
  "dependencies": {
    "axios": "^1.4.0"
  }
}
